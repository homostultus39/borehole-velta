# borehole-velta

Скрипт для определения номеров скважин в файлах .dwg. Автоматически устанавливает опорную скважину с относительной высотой (0,0) и рассчитывает относительные высоты других скважин относительно неё. Результаты выводятся в консоль в удобном формате.

## Возможности

- 🔍 Автоматическое определение номеров скважин в .dwg файлах
- 📐 Установка опорной скважины с относительной высотой 0
- 📊 Расчет относительных высот всех скважин
- 🖥️ Красивый вывод результатов в консоль
- 🎯 Гибкая настройка поиска скважин
- 📝 Подробное логирование процесса

## Установка

1. Убедитесь, что у вас установлен Python 3.13+
2. Установите зависимости:
```bash
poetry install
```

## Использование

### Командная строка

```bash
# Базовое использование
python src/main.py path/to/file.dwg

# С указанием опорной скважины
python src/main.py path/to/file.dwg -r 5

# С настройкой Z-координаты опорной скважины
python src/main.py path/to/file.dwg -r 5 -z 100.0

# С отладочным логированием
python src/main.py path/to/file.dwg -l DEBUG
```

### Программное использование

```python
from src.main import process_dwg_file

# Обработка файла
success = process_dwg_file(
    file_path="path/to/file.dwg",
    reference_borehole="5",  # Номер опорной скважины
    reference_z=100.0        # Z-координата опорной скважины
)
```

## Структура проекта

```
src/
├── __init__.py              # Инициализация пакета
├── main.py                  # Основной скрипт
├── autocad_handler.py       # Работа с AutoCAD
├── borehole_processor.py    # Обработка скважин
├── console_output.py        # Вывод в консоль
└── config.py               # Конфигурация
```

## Вывод результатов

Скрипт выводит результаты в консоль в следующем формате:

- 📊 **Сводка** - количество найденных скважин, опорная скважина, статистика по высотам
- 🎯 **Информация об опорной скважине** - координаты и параметры
- 📋 **Таблица скважин** - детальная информация о всех скважинах
- 📈 **Статистика обработки** - количество обработанных объектов

## Лог файл

- `borehole_analysis.log` - Подробный лог процесса обработки

## Требования

- Python 3.13+
- AutoCAD (для работы с .dwg файлами)
- pyautocad

## Поддерживаемые форматы поиска скважин

Скрипт распознает следующие форматы номеров скважин:
- "скв. 123", "скважина 123"
- "№ 123"
- "123 скв"
- "скв 123"
- Просто число "123"

## Логирование

Логи сохраняются в файл `borehole_analysis.log` и выводятся в консоль. Уровень логирования можно настроить через параметр `-l`.
